#if !defined(API_H)
#define API_H

#define MAX_SOCKPATH_LEN 1024


//variabile globale per abilitare la stampa sul stdout per ogni operazione
int print; 


int saveFile(char* dirSaving, char* path, char* content, size_t contentsize);

int receiveFiles(int fd,  char* dirname, int nFiles);

/*----- operazioni per le richieste -----*/ 

int openconnectionAPI(char *sockname, int msec, struct timespec abstime);

int closeconnectionAPI(char *sockname);

int openfileAPI(char* path, int flags);

int readfileAPI(char* path, void** bufRead, int *bytesRead);

int readnfileAPI(int nFiles, char* dirSaving);

int writefileAPI(char* path, char* dirSaving);

int appendtofileAPI(char* path, char* content, size_t size, char* dirSaving);

int lockfileAPI(char* path);

int unlockfileAPI(char* path);

int closefileAPI(char* path);

int removefileAPI(char* path);

#endif
